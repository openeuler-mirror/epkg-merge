buildRequires:
- gcc, perl, sed, coreutils, dejagnu, zlib-devel, glibc-static, sharutils, bc, libstdc++-static
- bison, m4, gcc-c++, gettext, flex, zlib-devel, texinfo >= 4.0, perl-podlators chrpath
buildRoot:
- '%(mktemp -ud %{_tmppath}/%{name}-%{version}-%{release}-XXXXXX)'
files: |
  %doc README
  %license COPYING3 COPYING COPYING3.LIB COPYING.LIB
  %{_bindir}/[!l]*
  %{_bindir}/ld.*
  %ghost %{_bindir}/ld
  %{_libdir}/lib*.so
  %{_libdir}/libctf*
  %{_libdir}/bfd-plugins/libdep.so
  %exclude %{_libdir}/libctf.a
  %exclude %{_libdir}/libctf-nobfd.a
  %exclude %{_libdir}/libbfd.so
  %exclude %{_libdir}/libopcodes.so
  %config(noreplace) /etc/ld.so.conf.d/*
meta.description: |
  The GNU Binutils are a collection of binary tools. The main ones are:
  ld - the GNU linker.
  as - the GNU assembler.
  addr2line - Converts addresses into filenames and line numbers.
  ar - A utility for creating, modifying and extracting from archives.
  c++filt - Filter to demangle encoded C++ symbols.
  dlltool - Creates files for building and using DLLs.
  gold - A new, faster, ELF only linker, still in beta test.
  gprof - Displays profiling information.
  nlmconv - Converts object code into an NLM.
  nm - Lists symbols from object files.
  objcopy - Copies and translates object files.
  objdump - Displays information from object files.
  ranlib - Generates an index to the contents of an archive.
  readelf - Displays information from any ELF format object file.
  size - Lists the section sizes of an object or archive file.
  strings - Lists printable strings from files.
  trip - Discards symbols.
  windmc - A Windows compatible message compiler.
  windres - A compiler for Windows resource files.
meta.homepage: https://sourceware.org/binutils
meta.license: GPLv3+
meta.summary: Binary utilities
name: binutils
patchset:
  '0': binutils-2.20.51.0.2-libtool-lib64.patch
  '1': export-demangle.h-in-devel-package.patch
  '10': bfd-Close-the-file-descriptor-if-there-is-no-archive.patch
  '11': binutils-AArch64-EFI.patch
  '12': backport-0001-PR28391-strip-objcopy-preserve-dates-.a-cannot-set-t.patch
  '13': 0001-x86-Add-int1-as-one-byte-opcode-0xf1.patch
  '14': 0002-x86-drop-OP_Mask.patch
  '15': 0003-x86-correct-VCVT-U-SI2SD-rounding-mode-handling.patch
  '16': 0004-x86-64-generalize-OP_G-s-EVEX.R-handling.patch
  '17': 0005-x86-64-properly-bounds-check-bnd-N-in-OP_G.patch
  '18': 0006-x86-fold-duplicate-register-printing-code.patch
  '19': 0007-x86-fold-duplicate-code-in-MOVSXD_Fixup.patch
  '2': binutils-2.22.52.0.4-no-config-h-check.patch
  '20': 0008-x86-correct-EVEX.V-handling-outside-of-64-bit-mode.patch
  '21': 0009-x86-drop-vex_mode-and-vex_scalar_mode.patch
  '22': 0010-x86-fold-duplicate-vector-register-printing-code.patch
  '23': 0011-x86-drop-xmm_m-b-w-d-q-_mode.patch
  '24': 0012-x86-drop-vex_scalar_w_dq_mode.patch
  '25': 0013-x86-drop-dq-b-d-_mode.patch
  '26': 0014-x86-express-unduly-set-rounding-control-bits-in-disa.patch
  '27': 0015-x86-Simplify-check-for-distinct-TMM-register-operand.patch
  '28': 0016-PATCH-1-2-Enable-Intel-AVX512_FP16-instructions.patch
  '29': 0017-PATCH-2-2-Add-tests-for-Intel-AVX512_FP16-instructio.patch
  '3': binutils-2.27-aarch64-ifunc.patch
  '30': 0018-x86-ELF-fix-.tfloat-output.patch
  '31': 0019-x86-ELF-fix-.ds.x-output.patch
  '32': 0020-x86-ELF-fix-.tfloat-output-with-hex-input.patch
  '33': 0021-x86-introduce-.hfloat-directive.patch
  '34': 0022-x86-Avoid-abort-on-invalid-broadcast.patch
  '35': 0023-x86-Put-back-3-aborts-in-OP_E_memory.patch
  '36': 0024-x86-Print-bad-on-invalid-broadcast-in-OP_E_memory.patch
  '37': 0025-x86-Terminate-mnemonicendp-in-swap_operand.patch
  '38': 0026-opcodes-Make-i386-dis.c-thread-safe.patch
  '39': 0027-x86-reduce-AVX512-FP16-set-of-insns-decoded-through-.patch
  '4': CVE-2019-1010204.patch
  '40': 0028-x86-reduce-AVX512-FP-set-of-insns-decoded-through-ve.patch
  '41': 0029-x86-consistently-use-scalar_mode-for-AVX512-FP16-sca.patch
  '42': backport-CVE-2022-38126.patch
  '43': backport-0001-texi2pod.pl-add-no-op-no-split-option-support-PR2814.patch
  '44': backport-AArch64-Add-support-for-AArch64-EFI-efi-aarch64.patch
  '45': backport-Add-support-for-AArch64-EFI-efi-aarch64.patch
  '46': backport-don-t-over-align-file-positions-of-PE-executable-sec.patch
  '5': Fix-a-potential-use-of-an-uninitialised-value-in-the.patch
  '6': backport-CVE-2021-45078.patch
  '7': backport-0001-CVE-2021-42574.patch
  '8': backport-0002-CVE-2021-42574.patch
  '9': backport-0003-CVE-2021-42574.patch
phase.build: |+
  CARGS=
  case %{_target_platform} in i?86*|arm*|aarch64*|riscv64*)
  CARGS="$CARGS --enable-64-bit-bfd"
  ;;
  esac

  case %{_target_platform} in x86_64*|i?86*|aarch64*|riscv64*)
  CARGS="$CARGS --enable-targets=x86_64-pep --enable-relro=yes"
  ;;
  esac

  export CFLAGS="$RPM_OPT_FLAGS"
  export LDFLAGS=$RPM_LD_FLAGS

  %configure --enable-ld \
  --with-sysroot=/ \
  --enable-deterministic-archives=no \
  --enable-lto \
  --enable-compressed-debug-sections=none \
  --enable-generate-build-notes=no \
  --enable-plugins \
  --enable-shared $CARGS \
    --quiet \
    --build=%{_target_platform} --host=%{_target_platform} \
    --target=%{_target_platform} \
  %if %{with gold}
  --enable-gold=default \
  %endif

  %make_build %{_smp_mflags} tooldir=%{_prefix} all
  %make_build %{_smp_mflags} tooldir=%{_prefix} info

phase.check: |+
  make -k check < /dev/null || :
  cat {gas/testsuite/gas,ld/ld,binutils/binutils}.sum
  for file in {gas/testsuite/gas,ld/ld,binutils/binutils}.{sum,log}
  do
  ln $file binutils-%{_target_platform}-$(basename $file) || :
  done
  tar cjf binutils-%{_target_platform}.tar.bz2 binutils-%{_target_platform}-*.{sum,log}
  uuencode binutils-%{_target_platform}.tar.bz2 binutils-%{_target_platform}.tar.bz2
  rm -f binutils-%{_target_platform}.tar.bz2 binutils-%{_target_platform}-*.{sum,log}

phase.install: |+
  %make_install DESTDIR=%{buildroot}

  make prefix=%{buildroot}%{_prefix} infodir=%{buildroot}%{_infodir} install-info

  for library in bfd/libbfd.a libiberty/libiberty.a opcodes/libopcodes.a
  do
  install -m 644 $library %{buildroot}%{_libdir}
  done

  install -m 644 include/libiberty.h %{buildroot}%{_prefix}/include
  chmod +x %{buildroot}%{_libdir}/lib*.so*

  rm -f %{buildroot}%{_libdir}/lib{bfd,opcodes}.{so,la}

  chrpath --delete %{buildroot}%{_bindir}/{ld.bfd,objcopy,addr2line,gprof,size,strings,c++filt,strip,as,objdump,nm,ranlib,ar,readelf}
  chrpath --delete %{buildroot}%{_libdir}/libopcodes-%{version}.so
  chrpath --delete %{buildroot}%{_libdir}/libctf.so.0.0.0

  mkdir -p %{buildroot}/etc/ld.so.conf.d
  echo "%{_libdir}" > %{buildroot}/etc/ld.so.conf.d/%{name}-%{_arch}.conf

  %ifarch %{ix86} x86_64 arm
  sed -i -e '/^#include "ansidecl.h"/{p;s~^.*$~#include <bits/wordsize.h>~;}' \
      -e 's/^#define BFD_DEFAULT_TARGET_SIZE \(32\|64\) *$/#define BFD_DEFAULT_TARGET_SIZE __WORDSIZE/' \
      -e 's/^#define BFD_HOST_64BIT_LONG [01] *$/#define BFD_HOST_64BIT_LONG (__WORDSIZE == 64)/' \
      -e 's/^#define BFD_HOST_64_BIT \(long \)\?long *$/#if __WORDSIZE == 32\
  #define BFD_HOST_64_BIT long long\
  #else\
  #define BFD_HOST_64_BIT long\
  #endif/' \
      -e 's/^#define BFD_HOST_U_64_BIT unsigned \(long \)\?long *$/#define BFD_HOST_U_64_BIT unsigned BFD_HOST_64_BIT/' \
      %{buildroot}%{_prefix}/include/bfd.h
  %endif
  touch -r bfd/bfd-in2.h %{buildroot}%{_prefix}/include/bfd.h


  %ifarch x86_64
  tee %{buildroot}%{_libdir}/libbfd.so <<EOF
  /* GNU ld script */

  OUTPUT_FORMAT(elf64-x86-64)

  INPUT ( %{_libdir}/libbfd.a -liberty -lz -ldl )
  EOF

  tee %{buildroot}%{_libdir}/libopcodes.so <<EOF
  /* GNU ld script */

  OUTPUT_FORMAT(elf64-x86-64)

  INPUT ( %{_libdir}/libopcodes.a -lbfd )
  EOF
  %endif

  %ifarch aarch64
  tee %{buildroot}%{_libdir}/libbfd.so <<EOF
  /* GNU ld script */

  OUTPUT_FORMAT(elf64-littleaarch64)

  INPUT ( %{_libdir}/libbfd.a -liberty -lz -ldl )
  EOF

  tee %{buildroot}%{_libdir}/libopcodes.so <<EOF
  /* GNU ld script */

  OUTPUT_FORMAT(elf64-littleaarch64)

  INPUT ( %{_libdir}/libopcodes.a -lbfd )
  EOF
  %endif

  %ifarch riscv64
  tee %{buildroot}%{_libdir}/libbfd.so <<EOF
  /* GNU ld script */

  OUTPUT_FORMAT(elf64-littleriscv)

  INPUT ( %{_libdir}/libbfd.a -liberty -lz -ldl )
  EOF

  tee %{buildroot}%{_libdir}/libopcodes.so <<EOF
  /* GNU ld script */

  OUTPUT_FORMAT(elf64-littleriscv)

  INPUT ( %{_libdir}/libopcodes.a -lbfd )
  EOF
  %endif

  rm -f %{buildroot}%{_infodir}/dir
  rm -rf %{buildroot}%{_prefix}/%{_target_platform}

  %find_lang binutils
  %if %{with gold}
  for library in opcodes bfd gas gprof ld gold
  %else
  for library in opcodes bfd gas gprof ld
  %endif
  do
  %find_lang $library
  cat $library.lang >> binutils.lang
  done

phase.prep: |+
  %autosetup -n %{name}-%{version} -p1


  sed -i -e 's/%''{release}/%{release}/g' bfd/Makefile{.am,.in}
  sed -i -e '/^libopcodes_la_\(DEPENDENCIES\|LIBADD\)/s,$, ../bfd/libbfd.la,' opcodes/Makefile.{am,in}
  perl -pi -e 's/i\[3-7\]86/i[34567]86/g' */conf*

  sed -i -e '/pagesize/s/0x1000,/0x10000,/' gold/aarch64.cc
  sed -i -e '/#define.*ELF_COMMONPAGESIZE/s/0x1000$/0x10000/' bfd/elf*aarch64.c

  sed -i -e 's/^ PACKAGE=/ PACKAGE=/' */configure
  sed -i -e "2aDEJATOOL = binutils" binutils/Makefile.am
  sed -i -e "2aDEJATOOL = gas" gas/Makefile.am
  sed -i -e "2aDEJATOOL = ld" ld/Makefile.am
  sed -i -e "s/^DEJATOOL = .*/DEJATOOL = binutils/" binutils/Makefile.in
  sed -i -e "s/^DEJATOOL = .*/DEJATOOL = gas/" gas/Makefile.in
  sed -i -e "s/^DEJATOOL = .*/DEJATOOL = ld/" ld/Makefile.in

  touch gas/doc/as.texi
  touch */configure

provides:
- bundled(libiberty)
release: 12
requiresPost:
- info coreutils chkconfig
requiresPreUn:
- info chkconfig
rpmMacros:
- '%bcond_without gold %ifnarch riscv64'
- '%bcond_with gold %else %ifnarch riscv64'
- '%define _gnu %{nil}'
- '%{!?ld_bfd_priority: %global ld_bfd_priority    50}'
- '%{!?ld_gold_priority:%global ld_gold_priority   30} %if %{with gold}'
runtimePhase.post: |+
  %__rm -f %{_bindir}/ld
  %{_sbindir}/alternatives --install %{_bindir}/ld ld \
    %{_bindir}/ld.bfd %{ld_bfd_priority}

  %if %{with gold}
  %{_sbindir}/alternatives --install %{_bindir}/ld ld \
    %{_bindir}/ld.gold %{ld_gold_priority}
  %endif

  if [ $1 = 0 ]; then
  %{_sbindir}/alternatives --auto ld
  fi

  /sbin/ldconfig

runtimePhase.postun: |+
  /sbin/ldconfig

runtimePhase.preun: |+
  if [ $1 = 0 ]; then
  %{_sbindir}/alternatives --remove ld %{_bindir}/ld.bfd

  %if %{with gold}
  %{_sbindir}/alternatives --remove ld %{_bindir}/ld.gold
  %endif
  fi

source:
  '0': https://ftp.gnu.org/gnu/binutils/binutils-%{version}.tar.xz
subpackage:
  binutils-devel:
    files: |
      %{_prefix}/include/*
      %{_libdir}/lib*.a
      %{_libdir}/libbfd.so
      %{_libdir}/libopcodes.so
    meta.description: |
      The devel package contains BFD and opcodes static and dynamic libraries.
      The static libraries are used by the dynamic libraries which are linkier
      scripts imported from glibc/Makerules.
    meta.summary: devel package including header files and libraries.
    provides:
    - binutils-static = %{version}-%{release}
    requires:
    - zlib-devel, binutils = %{version}-%{release}, coreutils
    requiresPost:
    - info
    requiresPreUn:
    - info
  binutils-help:
    files: |
      %{_mandir}/man1/*
      %{_infodir}/as.info.gz
      %{_infodir}/binutils.info.gz
      %{_infodir}/gprof.info.gz
      %{_infodir}/ld.info.gz
      %{_infodir}/[^b]*info*
      %{_infodir}/binutils*info*
      %{_infodir}/bfd*info*
    filesInput: binutils.lang
    meta.description: The help package contains man files.
    meta.summary: binutils help
    runtimePhase.post: |
      for info in as.info.gz binutils.info.gz gprof.info.gz ld.info.gz
      do
      /sbin/install-info --info-dir=%{_infodir} %{_infodir}/$info
      done
    runtimePhase.postun: |
      if [ -e %{_infodir}/binutils.info.gz ]
      then
      for info in as.info.gz binutils.info.gz gprof.info.gz ld.info.gz
      do
      /sbin/install-info --delete --info-dir=%{_infodir} %{_infodir}/$info
      done
      fi
    runtimePhase.preun: |
      if [ $1 = 0 ]; then
      if [ -e %{_infodir}/binutils.info.gz ]
      then
      for info in as.info.gz binutils.info.gz gprof.info.gz ld.info.gz
      do
      /sbin/install-info --delete --info-dir=%{_infodir} %{_infodir}/$info
      done
      fi
      fi
version: 2.37
